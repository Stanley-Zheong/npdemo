# Playwright Specs

`health-smoke.spec.ts` is the only current Playwright spec candidate. It covers
the existing starter health vertical slice and is safe to run against a local
app once Playwright is installed.

V1 business specs must not be generated until their contract in
`test/contracts/` has `generation_ready: true`.
